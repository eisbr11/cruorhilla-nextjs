import Layout from '@components/Layout';
import CustomOfflineComponent from '@components/CustomOfflineContent';

const CustomOffline = () => (
  <Layout>
    <CustomOfflineComponent />
  </Layout>
);

export default CustomOffline;
